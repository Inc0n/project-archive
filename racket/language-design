

for example to define let

operators: '+' | '-' | '*' | '/'

let <binds> = <expr> <block-body> => (let binds 

let a = true {
    a
} => true

let (x y) = (1 2) {
    (list x y)
} => (1 2)


<var>.<expr>(<argument-lst>) => (expr var argument-lst)


